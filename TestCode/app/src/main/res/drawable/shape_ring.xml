<?xml version="1.0" encoding="utf-8"?>
<!--
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="ring" //圆环
    android:innerRadius="4dp" //内环半径
    android:innerRadiusRatio="1" //浮点型，以环的宽度比率来表示内环的半径
    android:thickness="3dp" //环的厚度
    android:thicknessRatio="2" //浮点型，以环的宽度比率来表示环的厚度
    android:useLevel="false"/> //boolean值，如果当做是LevelListDrawable使用时值为true，否则为false
-->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="ring"
    android:innerRadius="4dp"
    android:innerRadiusRatio="1"
    android:thickness="2dp"
    android:thicknessRatio="1"
    android:useLevel="false"
    android:dither="true">

    <solid android:color="@color/color_ceng"/>

    <size
        android:width="20dp"
        android:height="20dp"/>

</shape>